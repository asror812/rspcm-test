spring:
  datasource:
    hikari:
      maximum-pool-size: 5
      minimum-idle: 1
      connection-timeout: 30000
      validation-timeout: 5000
      max-lifetime: 1200000

    #url: ${DB_URL:jdbc:postgresql://157.230.232.117:5432/db}
    url: jdbc:postgresql://localhost:5432/db
    username: username
    password: password
    
  jpa:
    hibernate:
      ddl-auto: update

    properties:
      hibernate:
        format_sql: true
    open-in-view: true

  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:asror0812@gmail.com}
    password: ${MAIL_PASSWORD:mknb upzz zcxw fnzw}

    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

app:
  upload-dir: /opt/backend/uploads
  firebase:
    credentials-path: /opt/backend/firebase-service-account.json
